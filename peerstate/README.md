# PeerState Module

This module actively monitors and stores the operational state of nodes in the network (Topology, Health, Liveliness, Capabilities).
It is not for metrics logging, but rather a decision-making store that the `placement` module uses.
